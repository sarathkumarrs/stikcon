const player=document.querySelector(".player"),video=player.querySelector(".viewer"),toggle=player.querySelector(".toggle");function togglePlay(){video.paused?video.play():video.pause()}function updateButton(){}function scrub(e){const t=e.offsetX/progress.offsetWidth*video.duration;video.currentTime=t}video.addEventListener("click",togglePlay),video.addEventListener("play",updateButton),video.addEventListener("pause",updateButton),toggle.addEventListener("click",togglePlay);let mousedown=!1;